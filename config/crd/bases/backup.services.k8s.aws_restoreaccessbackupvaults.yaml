---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: restoreaccessbackupvaults.backup.services.k8s.aws
spec:
  group: backup.services.k8s.aws
  names:
    kind: RestoreAccessBackupVault
    listKind: RestoreAccessBackupVaultList
    plural: restoreaccessbackupvaults
    singular: restoreaccessbackupvault
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RestoreAccessBackupVault is the Schema for the RestoreAccessBackupVaults
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RestoreAccessBackupVaultSpec defines the desired state of
              RestoreAccessBackupVault.
            properties:
              backupVaultName:
                description: |-
                  The name of the backup vault to associate with an MPA approval team.

                  Regex Pattern: `^[a-zA-Z0-9\-\_]{2,50}$`
                type: string
              backupVaultTags:
                additionalProperties:
                  type: string
                description: Optional tags to assign to the restore access backup
                  vault.
                type: object
              creatorRequestID:
                description: |-
                  A unique string that identifies the request and allows failed requests to
                  be retried without the risk of executing the operation twice.
                type: string
              requesterComment:
                description: |-
                  A comment explaining the reason for requesting restore access to the backup
                  vault.
                type: string
              sourceBackupVaultARN:
                description: |-
                  The ARN of the source backup vault containing the recovery points to which
                  temporary access is requested.
                type: string
            required:
            - sourceBackupVaultARN
            type: object
          status:
            description: RestoreAccessBackupVaultStatus defines the observed state
              of RestoreAccessBackupVault
            properties:
              ackResourceMetadata:
                description: |-
                  All CRs managed by ACK have a common `Status.ACKResourceMetadata` member
                  that is used to contain resource sync state, account ownership,
                  constructed ARN for the resource
                properties:
                  arn:
                    description: |-
                      ARN is the Amazon Resource Name for the resource. This is a
                      globally-unique identifier and is set only by the ACK service controller
                      once the controller has orchestrated the creation of the resource OR
                      when it has verified that an "adopted" resource (a resource where the
                      ARN annotation was set by the Kubernetes user on the CR) exists and
                      matches the supplied CR's Spec field values.
                      https://github.com/aws/aws-controllers-k8s/issues/270
                    type: string
                  ownerAccountID:
                    description: |-
                      OwnerAccountID is the AWS Account ID of the account that owns the
                      backend AWS service API resource.
                    type: string
                  region:
                    description: Region is the AWS region in which the resource exists
                      or will exist.
                    type: string
                required:
                - ownerAccountID
                - region
                type: object
              conditions:
                description: |-
                  All CRs managed by ACK have a common `Status.Conditions` member that
                  contains a collection of `ackv1alpha1.Condition` objects that describe
                  the various terminal states of the CR and its backend AWS service API
                  resource
                items:
                  description: |-
                    Condition is the common struct used by all CRDs managed by ACK service
                    controllers to indicate terminal states  of the CR and its backend AWS
                    service API resource
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the Condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              creationDate:
                description: |-
                  >The date and time when the restore access backup vault was created, in Unix
                  format and Coordinated Universal Time
                format: date-time
                type: string
              restoreAccessBackupVaultName:
                description: |-
                  The name of the created restore access backup vault.

                  Regex Pattern: `^[a-zA-Z0-9\-\_]{2,50}$`
                type: string
              vaultState:
                description: The current state of the restore access backup vault.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
